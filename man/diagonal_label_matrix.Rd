% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_sim.R
\name{diagonal_label_matrix}
\alias{diagonal_label_matrix}
\title{Create a Diagonal Label Comparison Matrix (Element-wise)}
\usage{
diagonal_label_matrix(
  a,
  b,
  type = c("s", "d"),
  dim1 = length(a),
  dim2 = length(b)
)
}
\arguments{
\item{a}{A vector of labels for the first set of data points.}

\item{b}{A vector of labels for the second set of data points. Must have same length as `a`.}

\item{type}{A character specifying the comparison type: "s" for same labels (a[i] == b[i])
or "d" for different labels (a[i] != b[i]). Default is "s".}

\item{dim1}{The row dimension of the output matrix (default: length(a)).}

\item{dim2}{The column dimension of the output matrix (default: length(b)).}
}
\value{
A sparse diagonal matrix where entry (i, i) is 1 if the labels at position i
  satisfy the comparison, 0 otherwise.
}
\description{
Compares labels at corresponding positions (element-wise) between two equal-length
vectors `a` and `b`. Creates a sparse matrix with entries only on the diagonal,
where position (i, i) is 1 if `a[i]` and `b[i]` satisfy the comparison.
}
\details{
This function performs element-wise comparison, NOT all-pairs comparison.
For all-pairs comparison (block structure), use \code{\link{binary_label_matrix}}.

The vectors `a` and `b` must have the same length. If they differ, recycling
will occur which is likely unintended.
}
\examples{
a <- factor(c("x","y","x"))
b <- factor(c("x","x","y"))
diagonal_label_matrix(a, b, type="d")

}
\seealso{
\code{\link{binary_label_matrix}} for all-pairs comparison
}
