% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searcher.R
\name{adjacency.nn_search}
\alias{adjacency.nn_search}
\title{Create Adjacency Matrix from nnsearch Object}
\usage{
\method{adjacency}{nn_search}(
  x,
  idim = nrow(x$indices),
  jdim = max(x$indices),
  return_triplet = FALSE,
  ...
)
}
\arguments{
\item{x}{An object of class "nnsearch".}

\item{idim}{The number of rows in the resulting matrix.}

\item{jdim}{The number of columns in the resulting matrix.}

\item{return_triplet}{Logical; whether to return triplet format.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
A sparse Matrix representing the adjacency matrix.
}
\description{
Convert a nearest neighbor search result to a sparse adjacency matrix.
}
\examples{
res <- list(indices = matrix(c(2L,1L), nrow=2),
            distances = matrix(c(0.1,0.2), nrow=2))
class(res) <- "nn_search"
attr(res,"len") <- 2; attr(res,"metric") <- "l2"
adjacency(res, idim=2, jdim=2)
}
