---
phase: 01-package-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - R/label_sim.R
  - R/class_graph.R
  - tests/testthat/test-label-similarity.R
  - tests/testthat/test-label-sim-extended.R
  - tests/testthat/test-class-graph-extended.R
  - NAMESPACE
  - man/diagonal_label_matrix.Rd
  - man/diagonal_label_matrix_na.Rd
  - man/discriminating_similarity.Rd
autonomous: true

must_haves:
  truths:
    - "Deprecated function label_matrix is completely removed from codebase"
    - "Deprecated function label_matrix2 is completely removed from codebase"
    - "Deprecated function discriminating_simililarity (typo) is completely removed from codebase"
    - "NAMESPACE no longer exports deprecated functions"
    - "Tests for deprecated functions are removed"
  artifacts:
    - path: "R/label_sim.R"
      provides: "Clean label functions without deprecated aliases"
    - path: "R/class_graph.R"
      provides: "Clean discriminating functions without deprecated alias"
    - path: "NAMESPACE"
      provides: "Updated exports without deprecated functions"
  key_links:
    - from: "NAMESPACE"
      to: "R/label_sim.R"
      via: "roxygen2 @export tags"
      pattern: "export\\(diagonal_label_matrix"
---

<objective>
Remove all deprecated function definitions, their documentation, and their tests from the package.

Purpose: Clean API surface for CRAN submission. These functions have been deprecated (using `.Deprecated()`) and internal code has been updated to use current names. Now safe to remove completely.

Output: Package with clean API containing only current function names.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-cleanup/01-RESEARCH.md
@.planning/phases/01-package-cleanup/01-01-SUMMARY.md
@R/label_sim.R
@R/class_graph.R
@tests/testthat/test-label-similarity.R
@tests/testthat/test-label-sim-extended.R
@tests/testthat/test-class-graph-extended.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated function definitions</name>
  <files>R/label_sim.R, R/class_graph.R</files>
  <action>
Remove these function definitions completely (including roxygen2 comments):

**R/label_sim.R - Remove label_matrix2 (lines 144-150):**
Delete the entire block:
```r
#' @rdname diagonal_label_matrix
#' @export
label_matrix2 <- function(a, b, type = c("s", "d"), dim1 = length(a),
                          dim2 = length(b)) {
  .Deprecated("diagonal_label_matrix")
  diagonal_label_matrix(a, b, type, dim1, dim2)
}
```

**R/label_sim.R - Remove label_matrix (lines 262-270):**
Delete the entire block:
```r
#' @rdname diagonal_label_matrix_na
#' @export
label_matrix <- function(a, b, type = c("s", "d"),
                         return_matrix = TRUE,
                         dim1 = length(a),
                         dim2 = length(b)) {
  .Deprecated("diagonal_label_matrix_na")
  diagonal_label_matrix_na(a, b, type, return_matrix, dim1, dim2)
}
```

**R/class_graph.R - Remove discriminating_simililarity (lines 450-456):**
Delete the entire block:
```r
#' @rdname discriminating_similarity
#' @export
discriminating_simililarity <- function(X, k, sigma, cg, threshold=.01) {

  .Deprecated("discriminating_similarity")
  discriminating_similarity(X, k, sigma, cg, threshold)
}
```

Note: Do NOT modify any other code in these files. Only remove the deprecated function blocks.
  </action>
  <verify>
Run: `grep -n "label_matrix2\|label_matrix\|discriminating_simililarity" R/label_sim.R R/class_graph.R | grep -v "diagonal_label_matrix"`
Expected: No matches (deprecated definitions removed)
  </verify>
  <done>All three deprecated function definitions are removed from R source files</done>
</task>

<task type="auto">
  <name>Task 2: Remove tests for deprecated functions</name>
  <files>tests/testthat/test-label-similarity.R, tests/testthat/test-label-sim-extended.R, tests/testthat/test-class-graph-extended.R</files>
  <action>
Remove test blocks that test deprecated functions:

**tests/testthat/test-label-similarity.R - Remove lines 84-95:**
Delete the test block:
```r
test_that("label_matrix works correctly", {
  labels1 <- c('A', 'B', 'A')
  labels2 <- c('A', 'A', 'B')

  result_same <- label_matrix(labels1, labels2, type = 's')
  result_diff <- label_matrix(labels1, labels2, type = 'd')

  expect_true(inherits(result_same, "Matrix"))
  expect_true(inherits(result_diff, "Matrix"))
  expect_equal(dim(result_same), c(3, 3))
  expect_equal(dim(result_diff), c(3, 3))
})
```

**tests/testthat/test-label-sim-extended.R - Remove lines 52-61:**
Delete the test block for label_matrix2 (including comment header):
```r
# ---------- label_matrix2 (deprecated alias) ----------

test_that("label_matrix2 calls diagonal_label_matrix with deprecation warning", {
  ...
})
```

**tests/testthat/test-label-sim-extended.R - Remove lines 137-146:**
Delete the test block for label_matrix (including comment header):
```r
# ---------- label_matrix (deprecated alias) ----------

test_that("label_matrix calls diagonal_label_matrix_na with deprecation", {
  ...
})
```

**tests/testthat/test-class-graph-extended.R - Remove lines 231-242:**
Delete the test block:
```r
test_that("discriminating_simililarity deprecated alias works", {
  ...
})
```

Note: Keep all other tests intact. Only remove tests that directly test the deprecated function aliases.
  </action>
  <verify>
Run: `grep -rn "label_matrix2\|\"label_matrix\|discriminating_simililarity" tests/testthat/ | grep -v "diagonal_label_matrix"`
Expected: No matches in test files (tests for deprecated functions removed)
  </verify>
  <done>All tests for deprecated functions are removed from test files</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate documentation and verify package</name>
  <files>NAMESPACE, man/*.Rd</files>
  <action>
Run roxygen2 to regenerate NAMESPACE and documentation:

1. Run `R -e "devtools::document()"` to:
   - Remove deprecated function exports from NAMESPACE
   - Remove @rdname aliases from .Rd files

2. Run `R -e "devtools::test()"` to verify all remaining tests pass

3. Run `R -e "devtools::check()"` to verify full package check passes

The roxygen2 run will automatically:
- Remove `export(label_matrix)` from NAMESPACE
- Remove `export(label_matrix2)` from NAMESPACE
- Remove `export(discriminating_simililarity)` from NAMESPACE
- Update man/diagonal_label_matrix.Rd to remove label_matrix2 alias
- Update man/diagonal_label_matrix_na.Rd to remove label_matrix alias
- Update man/discriminating_similarity.Rd to remove typo alias
  </action>
  <verify>
Run: `grep -E "label_matrix|label_matrix2|discriminating_simililarity" NAMESPACE`
Expected: No matches (exports removed)

Run: `R -e "devtools::check()" 2>&1 | grep -E "ERROR|WARNING|NOTE"`
Expected: Only pre-existing warnings (compiler flag noise), no new issues
  </verify>
  <done>NAMESPACE updated, documentation regenerated, package check passes</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -rn "label_matrix2\|discriminating_simililarity" R/` returns no matches
2. `grep -n "^label_matrix <-" R/label_sim.R` returns no matches
3. `grep "export.*label_matrix\|export.*discriminating_simililarity" NAMESPACE` returns no matches
4. `R -e "devtools::test()"` passes all tests
5. `R -e "devtools::check()"` shows no new errors or warnings
</verification>

<success_criteria>
- R/label_sim.R contains no `label_matrix` or `label_matrix2` function definitions
- R/class_graph.R contains no `discriminating_simililarity` function definition
- NAMESPACE does not export any of the three deprecated functions
- All tests pass (deprecated function tests removed)
- devtools::check() shows no new issues from function removal
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-cleanup/01-02-SUMMARY.md`
</output>
