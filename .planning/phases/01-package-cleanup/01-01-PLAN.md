---
phase: 01-package-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/knn_weights.R
  - R/class_graph.R
  - .Rbuildignore
autonomous: true

must_haves:
  truths:
    - "Internal code uses only current function names (diagonal_label_matrix, diagonal_label_matrix_na)"
    - ".planning directory is excluded from package build"
  artifacts:
    - path: "R/knn_weights.R"
      provides: "Internal code using diagonal_label_matrix_na"
      contains: "diagonal_label_matrix_na"
    - path: "R/class_graph.R"
      provides: "Internal code using diagonal_label_matrix"
      contains: "diagonal_label_matrix(labels, labels)"
    - path: ".Rbuildignore"
      provides: ".planning exclusion pattern"
      contains: "^\\.planning"
  key_links:
    - from: "R/knn_weights.R"
      to: "R/label_sim.R"
      via: "function call"
      pattern: "diagonal_label_matrix_na"
    - from: "R/class_graph.R"
      to: "R/label_sim.R"
      via: "function call"
      pattern: "diagonal_label_matrix\\(labels"
---

<objective>
Update internal code to use current function names and add .Rbuildignore entry for .planning directory.

Purpose: Prepare codebase for deprecated function removal by eliminating internal dependencies on deprecated aliases. This must happen BEFORE removing the deprecated functions to avoid breaking the package.

Output: Internal code updated, .Rbuildignore configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-cleanup/01-RESEARCH.md
@R/knn_weights.R
@R/class_graph.R
@R/label_sim.R
@.Rbuildignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update internal code to use current function names</name>
  <files>R/knn_weights.R, R/class_graph.R</files>
  <action>
Update R/knn_weights.R line 185:
- Change: `label_matrix(des[[nm]], des[[nm]]) * wt`
- To: `diagonal_label_matrix_na(des[[nm]], des[[nm]]) * wt`

Update R/class_graph.R lines 369-370:
- Change: `Ww <- label_matrix2(labels, labels)`
- To: `Ww <- diagonal_label_matrix(labels, labels)`
- Change: `Wb <- label_matrix2(labels, labels, type = "d")`
- To: `Wb <- diagonal_label_matrix(labels, labels, type = "d")`

Note: These are simple search-and-replace operations. The function signatures are identical between deprecated and current versions.
  </action>
  <verify>
Run: `grep -n "label_matrix\|label_matrix2" R/knn_weights.R R/class_graph.R`
Expected: No matches (internal usage removed)
  </verify>
  <done>R/knn_weights.R and R/class_graph.R contain no calls to deprecated function names</done>
</task>

<task type="auto">
  <name>Task 2: Add .planning to .Rbuildignore</name>
  <files>.Rbuildignore</files>
  <action>
Add the following line to .Rbuildignore:
```
^\.planning$
```

This Perl regex pattern:
- `^` anchors to package root
- `\.` escapes the literal dot
- `planning` matches the directory name
- `$` ensures exact match (not substring)

Place at end of file after existing entries.
  </action>
  <verify>
Run: `grep "planning" .Rbuildignore`
Expected: `^\.planning$` appears in output
  </verify>
  <done>.Rbuildignore contains pattern to exclude .planning directory from package builds</done>
</task>

<task type="auto">
  <name>Task 3: Verify package still builds and tests pass</name>
  <files>None (verification only)</files>
  <action>
Run R package tests to confirm internal code updates work correctly:

1. Run `R -e "devtools::test()"` to verify all tests pass
2. Check that no errors occur related to undefined functions

This verifies the replacement functions are correctly called before we remove the deprecated versions.
  </action>
  <verify>
Run: `R -e "devtools::test()" 2>&1 | tail -20`
Expected: All tests pass, no errors about missing functions
  </verify>
  <done>Package tests pass with updated internal function calls</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -rn "label_matrix\|label_matrix2" R/knn_weights.R R/class_graph.R` returns no matches
2. `grep "planning" .Rbuildignore` shows the exclusion pattern
3. `R -e "devtools::test()"` shows all tests passing
</verification>

<success_criteria>
- R/knn_weights.R uses `diagonal_label_matrix_na` instead of `label_matrix`
- R/class_graph.R uses `diagonal_label_matrix` instead of `label_matrix2`
- .Rbuildignore contains `^\.planning$` pattern
- All package tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-cleanup/01-01-SUMMARY.md`
</output>
